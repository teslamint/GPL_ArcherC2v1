int main(int ac, char **av)
{
  /* This is just a dummy test program acting as a placeholder for
     LDAP regression test (which are waiting for LDAP server side
     implementation, which is unlikely to happen) */
  return 0;
}
